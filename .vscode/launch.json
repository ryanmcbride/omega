{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debug (lldb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/omega",
            "preLaunchTask": "Build C++ project",
            "args": ["ryan"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/bin",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb"
        }
    ]
}